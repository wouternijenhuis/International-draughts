name: international_draughts
description: International Draughts - 10x10 board game (iOS + Android)
version: 1.0.0+3
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.10
  go_router: ^14.2.0
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2
  connectivity_plus: ^6.0.3
  fl_chart: ^0.68.0
  synchronized: ^3.1.0
  draughts_engine:
    path: ../shared/draughts-engine-dart

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
